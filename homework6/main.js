
/*var number = prompt('Enter the number from 1 to 10');
var randomNumber = Math.round (Math.random * 10);
var result = confirm('Do you want to try again?');

}*/
/*function roar (string, numOfLogs, callback ) {
    var currentNumber = numOfLogs || 1;
    for (var index = 0; index < currentNumber; index++) {
        console.log(string);

    }
var cb = roar(string);
if (callback) {
    cb();
}
}
 roar ('Hello function');
 roar ('Hello function 2');*/

 //Переписать/написать функцию обмена валют. 
// Выполнение функции должно быть следующего вида: exchange(from, to). 
 //Результатом ее выполнения должно быть число. 
 //Поместить предыдущее задание в ф-ю. 
 //Функция не должна обращаться к данным за своими пределами - только через аргументы.
 // Optional: Из второго задания разбить все возможные логически несвязанные блоки на отдельные функции. 
  //Optional: Написать функцию, повторяющую функционал метода forEach массива. Вызов должен выглядеть следующим образом: 

  /*var grn = prompt('Enter the sum in Hrivnas');
  console.log (grn);
  confirm ('The currency is 1$ = 28.5 hrn')
  var result = grn * 28.5;
  console.log (result);*/

  /*function exchange (from, to) {
    var from = prompt('Enter the sum in dollars');
    confirm ('The currency is 1$ = 28.5 hrn')
    var to = 28.5;
    return from * to
  }

 var func = function (result) {
     var result = exchange ();
     return result;
 }
 alert(func() + ' grn');

 fEach([1,2,3,4], function(num) { })*/

var arr = [1, 2, 3, 4];
function fEach (num, index, arr) {
    var string = 'Hello world';

}
console.log(fEach([1,2,3,4], function(num) { }));



